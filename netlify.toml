[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: qualquer rota cai no index.html e o React Router assume
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
